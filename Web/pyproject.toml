[project]
name = "web"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.120.4",
    "flask>=3.1.2",
    "flask-cors>=6.0.1",
    "uvicorn>=0.38.0",
]
